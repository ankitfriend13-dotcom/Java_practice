package testngdemo;

// Annotations are put with @ , whenever we want to use annotations, we need to import


/*
        1. @BeforeSuit/@AfterSuit   :   if we want to run something, before/After entire suit - multiple pkg
        2. @BeforeTest/@AfterTest   :   If we want to run something, before/After multiple test case file
        3. @BeforeClass/@AfterClass :   if we want to run something, before/After entire class
        4. BeforeMethod/@AfterMethod :  If we want to run something, before/after each method/ test
        5. @Test        :
 */

public class TestNgAnnotationsDemo {
}
