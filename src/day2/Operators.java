package day2;

// Arithmetic operators : + - * / %

// Relational operators : > < <= >= == !=  --- only works between two variables with any type of data types

// Logical operators : && || !      -- only works with boolean data types

// Increment operators & decrement operators : ++ --        also comes under unary operator

// Assignment operators : = += -= *= /=

// conditional / ternary operator  ?:


// Unary operator : can be used with only variables, ex ++ , --,=, +=, -=, *=, /=, !
// Binary operator : all arithmetic, relational, logical
// Ternary operator :  three variables are required ?:

public class Operators {
}







