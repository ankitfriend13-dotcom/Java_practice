package day1;

/*
        Compile time ::
        when we build or compile the code, that means the javac (compiler),
        converts the code into bytecode
        this time syntax errors, missing semicolons, incorrect data types are checked
        or unhandled expections

 */

/*
        Run time ::
        When run the code JVM (java virtual machine) loads the .class files and executes it line by line
        > Memory allocation for object, business logic and program interacts with outside systems
        > Run time error : code looks perfectly to the complier but something unexpected happens
 */

public class compileVsRun {
}
