package day5;

//  Built-in methods for string:
//      1. s.length()
//      2. s1.concat(s2)    s1.concat(s2).concat(s3)    -- joining two strings
//      3. s.trim() -- removes spaces from left and right
//      4. s.charAt(index)       -- returns a character from string based on index
//      5. s1.contains(s2)     --  returns boolean after checking if one string is contains the other string
//      6. s1.equals(s2)    --- compare two strings
//      7. s1.equalsIgnoreCase(s2)      -- Ignore the case and then compares
//      8. s1.replace(char, s2)     s1.replace(a,x).replace(c,y)      --- replace single/multiple(sequence) of characters in a string
//      9. s1.substring(start_index, end_index)       --- fetching a part of string
//      10. s1.toUpperCase() / toLowerCase()        -- coverts entire string into upper or lower case
//      11. s1.split("@")       -- split the strings based on delimiter -- after splitting we can store into array
//      Only few symbols we can use as delimiter -- cant use - * % ^ & ( )
//      12. s1.toCharArray();       -- convert the String into char[]


public class WorkingWithStrings {
    public static void main(String[] args){

    }
}
